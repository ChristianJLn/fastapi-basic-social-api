name: Build and Deploy Code

on: [push, pull_request]
  # push:
  #   branches: ["main"]
  # pull_request:
  #   branches:
  #     - main
# env:
#   DB_HOSTNAME: basic-social-postgres
#   DB_PORT: 5432
#   DB_PASSWORD: jfSAi8445bi2
#   DB_NAME: fastapi-basic-social-db
#   DB_USERNAME: postgres
#   SECRET_KEY: nnga78dfsa79dfsao893485j66h868756j654h6k3h2by34n2n5h3425645jh794537
#   ALGORITHM: HS256
#   ACCESS_TOKEN_EXPIRE_MINUTES: 60
jobs:
  job1:
    env:
      DB_HOSTNAME: basic-social-postgres
      DB_PORT: 5432
      DB_PASSWORD: jfSAi8445bi2
      DB_NAME: fastapi-basic-social-db
      DB_USERNAME: postgres
      SECRET_KEY: nnga78dfsa79dfsao893485j66h868756j654h6k3h2by34n2n5h3425645jh794537
      ALGORITHM: HS256
      ACCESS_TOKEN_EXPIRE_MINUTES: 60
    runs-on: ubuntu-latest
    steps:
      - name: Pulling git repo
        uses: actions/checkout@v6
      # - name: Say hi
      #   run: echo "Hi"
      - name: Set Up python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"
      - name: Update pip
        run: python -m pip install --upgrade pip
      - name: Install dependencies
        run: pip install -r requirements.txt
      - name: Run tests with pytest
        run: |
          pip install pytest
          pytest -v
